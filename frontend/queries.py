# queries.py

# Add your SQL query definitions or data query functions here.

# Example placeholder function:

def get_dashboard_queries():
    return {
        "summary": "SELECT * FROM dashboard_summary;",
        "details": "SELECT * FROM dashboard_details;",
    }
